{
    "name": "job-listing-browser",
    "version": "1.0.0",
    "description": "Chrome extension to automatically browse job listings",
    "main": "background.js",
    "scripts": {
      "build": "mkdir -p dist && cp manifest.json popup.html popup.js content.js background.js dist/ && mkdir -p dist/images",
      "package": "npm run build && cd dist && zip -r ../job-listing-browser.zip *"
    },
    "author": "",
    "license": "MIT",
    "devDependencies": {
      "webpack": "^5.81.0",
      "webpack-cli": "^5.0.2"
    }
  }